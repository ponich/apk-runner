version: '3.8'

services:
    ubuntu-24-04:
        build:
            context: .
            dockerfile: Dockerfile
        image: ubuntu-24-04-base
        container_name: ubuntu-24-04-container
        tty: true
        stdin_open: true
        volumes:
            - ./shared:/home/shared
        deploy:
            resources:
                limits:
                    cpus: '2'
                    memory: 2G
        networks:
            - my-network

networks:
    my-network:
        driver: bridge